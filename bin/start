#!/usr/bin/env ruby
# frozen_string_literal: true

require 'webrick'

root = File.expand_path(File.join('..', 'build'), __dir__)
server = WEBrick::HTTPServer.new(DocumentRoot: root)

trap('INT') { server.shutdown }
server.start
